{
	"name": "@produck/kit-package",
	"private": true,
	"scripts": {
		"deps:install": "npm -v && npm i && lerna bootstrap",
		"deps:clean": "lerna clean -y"
	},
	"devDependencies": {
		"lerna": "^5.5.4"
	}
}
